// src/constants/errors.ts

export const DB_CONSTRAINTS = {
  CATEGORY_IN_USE: "products_categoryId_fkey",
};

export const ERROR_MESSAGES = {
  CATEGORY_DELETE_IN_USE: "Category is in use and cannot be deleted.",
};
